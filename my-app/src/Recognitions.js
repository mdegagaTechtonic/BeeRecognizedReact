import React, { Component } from 'react';

//logout Component
class Recognitions extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      filterResponses: [],
    };
  }
  //renders everything after the navbar
  render () {
    return (
      <h1>testing Recognitions</h1>
    );
  }
}

export default Recognitions;
